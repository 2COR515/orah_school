// scripts/lesson-player.js
// Handles lesson player controls and Mark as Completed interaction

document.addEventListener('DOMContentLoaded', () => {
  // Placeholder: Mark as Completed button
  document.getElementById('mark-completed-btn').addEventListener('click', () => {
    alert('Lesson marked as completed! (Demo only)');
    // Here you could update UI, send to backend, etc.
  });

  // Previous/Next lesson buttons (stub)
  document.getElementById('prev-lesson-btn').addEventListener('click', () => {
    alert('Go to previous lesson (Demo only)');
  });
  document.getElementById('next-lesson-btn').addEventListener('click', () => {
    alert('Go to next lesson (Demo only)');
  });
});
